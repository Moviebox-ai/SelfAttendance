package com.aaryo.selfattendance.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════════
//  WALLET-INSPIRED COLOR PALETTE — SelfAttendance Pro
//  Inspired by: CoinGold, EmeraldGreen, SapphireBlue, CoralRed
// ═══════════════════════════════════════════════════════════════

// ─── LIGHT MODE ─────────────────────────────────────────────────

// Backgrounds — warm amber-tinted whites
val AppBackground  = Color(0xFFFFFBF0)   // warm cream white
val CardBackground = Color(0xFFFFFFFF)
val SoftSurface    = Color(0xFFFFF8E1)   // light amber surface
val DividerColor   = Color(0xFFFFE082)   // gold divider

// Text — warm dark tones
val TextPrimary   = Color(0xFF1A0E00)   // near-black warm brown
val TextSecondary = Color(0xFF8D6E63)   // warm muted brown

// Status / Accent Colors (matching WalletScreen exactly)
val GreenAccent  = Color(0xFF00C853)   // EmeraldGreen
val YellowAccent = Color(0xFFFFB300)   // CoinGold
val BlueAccent   = Color(0xFF1565C0)   // SapphireBlue
val PinkAccent   = Color(0xFFE53935)   // CoralRed

// ─── DARK MODE ──────────────────────────────────────────────────

// Backgrounds — deep warm dark amber
val DarkBackground    = Color(0xFF1A0E00)   // very dark warm brown
val DarkCardBackground= Color(0xFF2D1B00)   // dark amber card
val DarkSurface       = Color(0xFF3D2400)   // slightly lighter
val DarkDivider       = Color(0xFF5D3A00)   // muted gold divider

// Text
val DarkTextPrimary   = Color(0xFFFFF8E1)   // warm white
val DarkTextSecondary = Color(0xFFFFCC80)   // light amber muted

// Accent Colors — brighter for dark mode
val DarkGreenAccent  = Color(0xFF69F0AE)   // bright emerald
val DarkYellowAccent = Color(0xFFFFD54F)   // bright gold
val DarkBlueAccent   = Color(0xFF40C4FF)   // bright sapphire
val DarkPinkAccent   = Color(0xFFFF6E6E)   // bright coral
