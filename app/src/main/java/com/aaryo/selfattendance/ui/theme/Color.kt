package com.aaryo.selfattendance.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════════
//  DEEP VIOLET COLOR PALETTE — SelfAttendance Pro
//  Matches the app's default DEEP_VIOLET theme
// ═══════════════════════════════════════════════════════════════

// ─── LIGHT MODE ─────────────────────────────────────────────────

// Backgrounds — violet-tinted whites
val AppBackground  = Color(0xFFF5F3FF)   // violet-tinted white
val CardBackground = Color(0xFFFFFFFF)
val SoftSurface    = Color(0xFFEDE9FE)   // light violet surface
val DividerColor   = Color(0xFFDDD6FE)   // soft violet divider

// Text — deep violet tones
val TextPrimary   = Color(0xFF1E1B4B)   // deep violet near-black
val TextSecondary = Color(0xFF6B7280)   // muted gray

// Status / Accent Colors
val GreenAccent  = Color(0xFF00C853)   // EmeraldGreen
val YellowAccent = Color(0xFFFFB300)   // Amber
val BlueAccent   = Color(0xFF1565C0)   // SapphireBlue
val PinkAccent   = Color(0xFFE53935)   // CoralRed

// ─── DARK MODE ──────────────────────────────────────────────────

// Backgrounds — deep violet dark
val DarkBackground     = Color(0xFF0F0A2E)   // very dark violet
val DarkCardBackground = Color(0xFF1A1340)   // dark violet card
val DarkSurface        = Color(0xFF2D2469)   // slightly lighter violet
val DarkDivider        = Color(0xFF2D2460)   // muted violet divider

// Text
val DarkTextPrimary   = Color(0xFFEDE9FE)   // light violet white
val DarkTextSecondary = Color(0xFFB0A8CC)   // muted violet

// Accent Colors — brighter for dark mode
val DarkGreenAccent  = Color(0xFF69F0AE)   // bright emerald
val DarkYellowAccent = Color(0xFFFFD54F)   // bright gold
val DarkBlueAccent   = Color(0xFF40C4FF)   // bright sapphire
val DarkPinkAccent   = Color(0xFFFF6E6E)   // bright coral
